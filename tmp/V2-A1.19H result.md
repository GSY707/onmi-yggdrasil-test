# V2-A1.19H 执行结果

日期：2026-07-17

结论：A1.19H 通过，机器分类 `generalized_hybrid_core_confirmed`，允许进入 A1.20B。

## 完成项

H1 已删除 slot index 与固定 register identity 的绑定，使用 equality-only opaque handles、随机 slot mapping、共享 continuous transition 和 query-coupled answer。overfit32、三个 fresh formal 与 formal 后 causal/address intervention 均通过，formal/causal 为 `3/3`。

H2 已把训练实体数量扩展为 `N=2,3,4`，把 `N=5` 与 heldout relation 留作外推。三组独立数据 audit、overfit32、三个 fixed-4000 formal 和 formal 后 causal intervention 全部通过。三个 run 的 `N=5` 与 `N=5+relation` trajectory/final/answer 均为 `1.0`；OOD horizon 最低 aggregate trajectory 为 `0.983073`。

部署 artifact 全部物理删除训练辅助头；handle 没有语义 embedding；不存在固定前三槽语义、COPY/SWAP executor branch 或答案 metadata。

训练吞吐缺陷已修复。旧路径每步执行 Python→CUDA 标量编码和 recurrent CUDA 同步；新路径改为一次性 validated CPU encoding、GPU tensor cache、预生成采样和无热路径同步。60-step 对照加速 `10.744×`，final loss delta `0`，parameter max delta `0`。run-3 正式 4000-step 训练从旧量级约 15 分钟降到 `155.52s`。

## 执行中修正

- 固定 `entity_0...` 名称导致独立数据 support 不足，删除未训练数据并切换为随机 opaque lexical identifier。
- 第一版 overfit32 selector 遗漏 `N=4`，因此 Gate 失败；修正 selector 后 fresh 重跑通过。该失败属于实验选择器缺陷，不是模型证据。
- run-3 旧 checkpoint 在训练吞吐分析时停止并删除；数值等价基准与旧 checkpoint 全 split 回归通过后，才 fresh 重跑正式训练。

## 未完成项

- A1.20B learned full-text Boundary 尚未执行；当前 core 仍接收 exact-symbolic input。
- A1.21P matched direct/text-CoT/K1/multi-vector Pareto 尚未执行。
- 第二个关系/规划任务族尚未加入。
- A1.22A natural-language audit 与 architecture-fidelity formal 尚未执行。
- 因此完整 V2-A/R1 仍未通过，不得进入 V2-B。

下一步：只执行 A1.20B。冻结 A1.19H core，Boundary 使用完整 Qwen hidden，自主学习 source/target/query/relation/value/control；禁止 oracle span/role tensor、hard re-embedding 兼容补丁与任务专用分支。先过独立 Boundary formal，再决定是否运行短程联合。
