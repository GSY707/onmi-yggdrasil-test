# V2-A1.18/A1.18B result

正式结论：机制问题已解决，但只覆盖 exact-symbolic 三寄存器诊断 core。

- QAUX 与 FINAL-SAUX overfit32 均通过；QAUX 只作阳性控制，没有运行三 seed formal。
- FINAL-SAUX paired formal/causal 为 `2/3`：两个seed完整通过，一个seed从未进入算法盆地。
- TSAUX 把全局完整状态监督扩展到每一个递归步，使用跨步共享训练头，不读取 queried-answer 标签。
- TSAUX paired model seed `20261321/22/23`：formal/causal `3/3`。
- TSAUX fresh model seed `20261821/22/23`：formal/causal `3/3`。
- 六个 TSAUX deployment 的 causal trajectory/answer 都为 `1.0`；全部 counterfactual Gate 通过，disable-recurrence 与 wrong-start trajectory 都为 `0`。
- formal 前全部 `training_auxiliary_head.*` 参数已物理删除，部署模型正式答案只来自 query-coupled state。
- 机器分类：`per_step_global_state_credit_assignment_confirmed`；`mechanism_solved=true`。

直接根因不是“必须使用答案标签”，而是原目标缺少每一步 global workspace → complete state 的密集信用分配。FINAL-SAUX 只在最后一步提供该梯度，能够工作但 seed 不稳定；TSAUX 把它变成六 seed 可重复的训练协议。

仍未解决：每步完整 state target 在开放任务中的可扩展来源。本阶段完成时曾建议下一步做 A1.19 state-target source annealing；2026-07-17 路线重审已将其从硬门降级。白皮书现接受离散地址/控制 + 连续语义 payload 的混合 core，当前下一步改为 A1.19H generalized hybrid core；仍不直接进入旧 A2 或 V2-B。

机器真源：`artifacts/v2-a/a1_18b/assessment-summary.json`。
